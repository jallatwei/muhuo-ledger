<script setup lang="ts">
/**
 * 应用根组件
 * ============================================================
 * 这里**只**负责把路由渲染出来。
 *
 * ★ 外壳（侧边栏、顶栏、公司/期间切换）不在这里，而是分成两个布局组件：
 *     · layouts/AuthLayout.vue —— 登录、注册（没有"当前公司"这个概念）
 *     · layouts/AppShell.vue   —— 其余所有页面（需要主体与期间）
 *
 *   曾经的做法是在这里写 `v-if="已登录"` 包住整个界面。那样未登录时
 *   "当前主体/当前期间"全都不存在，外壳里每个控件都得再写一次 v-if，
 *   漏一个就是空控件或者直接报错。分成两棵路由子树之后，
 *   两类页面彻底互不干扰，新增页面也不必再考虑这个问题。
 */
</script>

<template>
  <router-view />
</template>
